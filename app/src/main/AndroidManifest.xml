<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ng.com.blogspot.httpofficialceo.fmtfuto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RatiosActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SimpleInterestActivity"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity
            android:name=".CompoundInterestActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".IntroductionActivity" />
        <activity
            android:name=".AboutDepartment"
            android:label="@string/title_activity_about_department"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LecturerDetails" />
        <activity
            android:name=".YearOneActivity"
            android:label="@string/year_one"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".DetailsActivity"></activity>
    </application>

</manifest>